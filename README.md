# Document-Merger
A simple Python tool that lets you merge multiple DOCX and PDF files into a single PDF using a file selection window. The program preserves formatting, allows custom file naming, and automatically saves the merged output to your Documents folder.
